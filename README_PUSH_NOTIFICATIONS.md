# Push уведомления для PWA

Интеграция push уведомлений для Progressive Web App (PWA) приложения.

## ✅ Что реализовано

### Клиентская часть (Frontend)

1. **Сервис для работы с push уведомлениями** (`src/core/services/pushNotificationService.ts`)

   - Подписка/отписка на push уведомления
   - Получение VAPID ключа
   - Управление разрешениями
   - Конвертация подписки в формат для отправки на сервер

2. **React хук** (`src/core/hooks/usePushNotifications.ts`)

   - Удобный интерфейс для работы с push уведомлениями в компонентах
   - Автоматическое управление состоянием подписки
   - Обработка ошибок

3. **UI компонент** (`src/core/components/ui/PushNotificationSettings.tsx`)

   - Компонент для управления настройками push уведомлений
   - Интегрирован в `ProfileModal`
   - Переключатель включения/выключения уведомлений
   - Отображение статуса подписки и разрешений

4. **API методы** (`src/modules/auth/api/index.ts`)

   - `getVapidPublicKey()` - получение VAPID публичного ключа
   - `savePushSubscription()` - сохранение подписки пользователя
   - `removePushSubscription()` - удаление подписки пользователя

5. **Service Worker обработчик** (`public/sw-push-handler.js`)
   - Обработка входящих push событий
   - Обработка кликов по уведомлениям
   - Кастомизация уведомлений

## 🔧 Что нужно настроить на сервере

### 1. Генерация VAPID ключей

Для работы push уведомлений нужна пара VAPID ключей (публичный и приватный).

**Установите библиотеку для работы с web-push (Node.js):**

```bash
npm install web-push
```

**Генерация ключей:**

```javascript
const webpush = require("web-push");

// Генерируем VAPID ключи (делайте это один раз)
const vapidKeys = webpush.generateVAPIDKeys();

console.log("Публичный ключ:", vapidKeys.publicKey);
console.log("Приватный ключ:", vapidKeys.privateKey);
console.log("Email (subject):", "mailto:your-email@example.com");
```

Сохраните ключи в переменные окружения:

```env
VAPID_PUBLIC_KEY=your-public-key
VAPID_PRIVATE_KEY=your-private-key
VAPID_SUBJECT=mailto:your-email@example.com
```

### 2. API Endpoints на сервере

Реализуйте следующие endpoints:

#### `GET /push/vapid-public-key`

Возвращает публичный VAPID ключ для подписки.

**Response:**

```json
{
  "publicKey": "BHx...ваш публичный ключ..."
}
```

**Пример реализации (Node.js/Express):**

```javascript
app.get("/push/vapid-public-key", (req, res) => {
  res.json({
    publicKey: process.env.VAPID_PUBLIC_KEY,
  });
});
```

#### `POST /push/subscribe`

Сохраняет push подписку пользователя.

**Request Body:**

```json
{
  "endpoint": "https://fcm.googleapis.com/fcm/send/...",
  "keys": {
    "p256dh": "base64-encoded-key",
    "auth": "base64-encoded-auth-secret"
  }
}
```

**Пример реализации:**

```javascript
app.post("/push/subscribe", authenticate, async (req, res) => {
  const { endpoint, keys } = req.body;
  const userId = req.user.id; // из middleware аутентификации

  // Сохраняем подписку в базу данных
  await db.pushSubscriptions.upsert({
    userId,
    endpoint,
    p256dh: keys.p256dh,
    auth: keys.auth,
  });

  res.status(200).json({ success: true });
});
```

#### `POST /push/unsubscribe`

Удаляет push подписку пользователя.

**Request Body:**

```json
{
  "endpoint": "https://fcm.googleapis.com/fcm/send/..."
}
```

**Пример реализации:**

```javascript
app.post("/push/unsubscribe", authenticate, async (req, res) => {
  const { endpoint } = req.body;
  const userId = req.user.id;

  await db.pushSubscriptions.destroy({
    where: { userId, endpoint },
  });

  res.status(200).json({ success: true });
});
```

### 3. Отправка push уведомлений

**Пример отправки уведомления пользователю:**

```javascript
const webpush = require("web-push");

// Настройка VAPID
webpush.setVapidDetails(
  process.env.VAPID_SUBJECT,
  process.env.VAPID_PUBLIC_KEY,
  process.env.VAPID_PRIVATE_KEY
);

// Функция отправки уведомления
async function sendPushNotification(userId, notificationData) {
  // Получаем подписки пользователя из БД
  const subscriptions = await db.pushSubscriptions.findAll({
    where: { userId },
  });

  const payload = JSON.stringify({
    title: notificationData.title || "ЧистоДом",
    body: notificationData.body,
    icon: "/favicon/android-chrome-192x192.png",
    badge: "/favicon/android-chrome-192x192.png",
    data: {
      url: notificationData.url || "/",
      ...notificationData.data,
    },
  });

  // Отправляем уведомления всем устройствам пользователя
  const promises = subscriptions.map((subscription) => {
    return webpush
      .sendNotification(
        {
          endpoint: subscription.endpoint,
          keys: {
            p256dh: subscription.p256dh,
            auth: subscription.auth,
          },
        },
        payload
      )
      .catch((error) => {
        // Если подписка невалидна, удаляем её
        if (error.statusCode === 410) {
          return db.pushSubscriptions.destroy({
            where: { id: subscription.id },
          });
        }
        console.error("Ошибка отправки push:", error);
      });
  });

  await Promise.all(promises);
}
```

**Пример использования:**

```javascript
// Отправка уведомления о новом заказе
await sendPushNotification(userId, {
  title: "Новый заказ",
  body: "Ваш заказ успешно создан",
  url: "/orders/123",
  data: { orderId: "123" },
});

// Отправка уведомления об изменении статуса заказа
await sendPushNotification(userId, {
  title: "Статус заказа обновлен",
  body: "Ваш заказ находится в пути",
  url: "/orders/123",
});
```

## 📱 Поддержка браузеров

- ✅ Chrome/Edge (Android & Desktop)
- ✅ Firefox (Android & Desktop)
- ✅ Safari (iOS 16.4+, macOS)
- ❌ Safari на iOS < 16.4 (не поддерживает Web Push API)

## 🔒 Безопасность

1. **VAPID ключи**: Храните приватный ключ только на сервере, никогда не передавайте его клиенту
2. **Аутентификация**: Все endpoints должны требовать аутентификацию пользователя
3. **Валидация**: Проверяйте формат подписки перед сохранением в БД
4. **Очистка**: Регулярно удаляйте невалидные подписки (статус 410)

## 🗄️ Схема базы данных

Рекомендуемая структура таблицы для хранения подписок:

```sql
CREATE TABLE push_subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  endpoint TEXT NOT NULL,
  p256dh TEXT NOT NULL,
  auth TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, endpoint)
);

CREATE INDEX idx_push_subscriptions_user_id ON push_subscriptions(user_id);
```

## 🧪 Тестирование

1. Установите PWA на устройство или используйте браузер в режиме разработчика
2. Откройте профиль пользователя
3. Включите переключатель "Push уведомления"
4. Разрешите уведомления в браузере
5. Отправьте тестовое уведомление с сервера
6. Проверьте получение уведомления

## 📝 Заметки

- Push уведомления работают даже когда приложение закрыто (если PWA установлен)
- На iOS требуется iOS 16.4+ и Safari
- Chrome автоматически обрабатывает push уведомления через Firebase Cloud Messaging (FCM)
- Firefox использует собственную службу push уведомлений

## 🐛 Решение проблем

### Уведомления не приходят

1. Проверьте, что VAPID ключи правильно настроены
2. Убедитесь, что подписка сохранена в БД
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что сервер использует правильный формат payload
5. Проверьте, что пользователь дал разрешение на уведомления

### Подписка не сохраняется

1. Проверьте аутентификацию пользователя
2. Проверьте формат данных подписки (endpoint, keys.p256dh, keys.auth)
3. Проверьте логи сервера на ошибки

### Уведомления приходят, но не отображаются

1. Проверьте service worker (sw-push-handler.js загружается)
2. Проверьте консоль service worker на ошибки
3. Убедитесь, что иконки существуют по указанным путям
